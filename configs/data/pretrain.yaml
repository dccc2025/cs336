source: fineweb-edu
language: en
# Chinchilla-ish for ~50M: ~1B tokens (sample-10BT is the raw pool)
target_tokens: 1000000000
stream: true
raw_dir: data/raw/fineweb
clean_dir: data/clean/pretrain
tokenized_dir: data/tokenized/pretrain
tokenizer_dir: data/tokenizer
shard_tokens: 16000000
